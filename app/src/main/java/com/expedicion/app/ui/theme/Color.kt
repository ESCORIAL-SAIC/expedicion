package com.expedicion.app.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta estilo "EKart" (compartida con la app hermana trazabilidad).
val ExpBlue = Color(0xFF1565C0)        // azul principal (barras, botones)
val ExpBlueDark = Color(0xFF0D47A1)    // azul oscuro (acentos, texto sobre container claro)
val ExpBlueLight = Color(0xFFD6E8FB)   // celeste de tiles/containers
val ExpSurfaceVar = Color(0xFFF2F5F9)  // gris muy claro para cards informativas
val ExpGreen = Color(0xFF2E7D32)       // acciones positivas (Nuevo, disponible)
val ExpRed = Color(0xFFC62828)         // acciones destructivas / no disponible
